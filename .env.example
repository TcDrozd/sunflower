# The public hostname you want (create an A record to the VM IP)
DOMAIN=sunflower.example.com
# Email for Let's Encrypt notifications
EMAIL=you@example.com

# Any Flask secrets/configs your app reads (example names)
FLASK_ENV=production
SECRET_KEY=change-me